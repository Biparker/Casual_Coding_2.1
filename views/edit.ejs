<%- include('partials/header') %>
</hr>
   <div class="jumbotron text-center">
     <div class="container">
      <h2 class="display-3">Revise Project</h2>
  
      <form action="/update/<%= user._id %>" method="POST">
      <div class="form-group">
        <label for="project">Project:</label>
        <input type="text" class="form-control" id="project" name="project" value="<%= user.project %>">
      </div>
      <div class="form-group">
        <label for="percent_done">Percent Done:</label>
        <input type="text" class="form-control" id="percent_done" name="percent_done" value="<%= user.percent_done %>">
      </div>
      <div class="form-group">
        <label for="description">Description:</label>
        <input type="text" class="form-control" id="description" name="description" value ="<%= user.description %>">
       </div>
      <button type="submit" class="btn btn-primary">Save Changes</button>
     </form>

    </div>
  </div>


<%- include('partials/footer') %>
